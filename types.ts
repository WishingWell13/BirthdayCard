export type Card = {
  id: string;
  recipientName: string;
  dob: string;
};
export type Card = {
  id: string;
  recipientName: string;
  dob: string;
};

export type Message = {
  id: string;
  cardId: string;
  authorName: string;
  messageText: string;
};
export type Message = {
  id: string;
  cardId: string;
  authorName: string;
  messageText: string;
};
